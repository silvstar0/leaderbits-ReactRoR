== render 'team_members/form'

div.text-center
  hr
  = link_to "#{@team_member.user.name} is no longer part of the team", team_member_path(@team_member, controller_name: params[:controller_name], action_name: params[:action_name]), method: 'delete', style: 'text-decoration: underline', data: { confirm: 'Are you sure to continue?' }, class: 'clear button alert'
